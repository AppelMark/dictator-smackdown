/**
 * Script to generate all audio files via ElevenLabs API.
 * Run with: npx tsx scripts/generate-audio.ts
 */

async function main(): Promise<void> {
  console.log('Audio generation script - placeholder');
  console.log('This script will generate all commentary and catchphrase audio via ElevenLabs.');
}

main().catch(console.error);
