/**
 * Script to combine individual sprites into Phaser atlas spritesheets.
 * Run with: npx tsx scripts/build-spritesheets.ts
 */

async function main(): Promise<void> {
  console.log('Spritesheet builder - placeholder');
  console.log('This script will combine sprites into Phaser atlas PNG + JSON files.');
}

main().catch(console.error);
