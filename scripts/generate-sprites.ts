/**
 * Script to generate all character sprites via Replicate API.
 * Run with: npx tsx scripts/generate-sprites.ts
 */

async function main(): Promise<void> {
  console.log('Sprite generation script - placeholder');
  console.log('This script will generate all 54 sprites via Replicate API.');
}

main().catch(console.error);
